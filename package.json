{
  "name": "fitcheckai",
  "version": "0.1.0",
  "private": true,
  "description": "FitCheck AI backend — Cloudflare Pages Functions (TypeScript, no build step)",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.1"
  }
}
